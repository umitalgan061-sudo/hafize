node_modules/
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
Thumbs.db
coverage/
*.log
*.pid
*.pid.lock
*.seed
*.enc
*.key
*.pem
*.crt
runtime/
data/

# The typed entries in public/typed-build/ are committed: the static server
# serves them directly and the check gate asserts every PWA shell asset exists
# on disk. Their sourcemaps are pure build output and stay untracked.
public/typed-build/*.map
