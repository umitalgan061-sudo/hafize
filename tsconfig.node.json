{
  "$schema": "https://json.schemastore.org/tsconfig",
  "// Neden": [
    "Sunucu runtime'ı ve kontrol paketleri. DOM lib'i bilerek yüklenmez:",
    "lib/ içindeki bir modülün document'a dokunması derleme hatası olmalıdır."
  ],
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"]
  },
  "include": ["server.mjs", "lib/**/*.mjs", "lib/**/*.ts", "scripts/**/*.mjs", "types/node.d.ts", "types/shared.d.ts"]
}
