{
  "name": "hafize-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Hafize kişisel yapay zekâ çalışma alanı — güvenli, Claude-benzeri frontend başlangıcı.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node scripts/run-checks.mjs",
    "check:list": "node scripts/run-checks.mjs --list",
    "precheck": "node scripts/run-checks.mjs --only=voice- && node scripts/run-checks.mjs --only=ui-shell && node scripts/run-checks.mjs --only=sidebar-"
  },
  "dependencies": {
    "redis": "^6.1.0"
  }
}
