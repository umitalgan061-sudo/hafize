{
  "name": "hafize-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Hafize kişisel yapay zekâ çalışma alanı — güvenli, Claude-benzeri frontend başlangıcı.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node scripts/run-checks.mjs",
    "check:syntax": "node scripts/run-checks.mjs --syntax-only",
    "check:tests": "node scripts/run-checks.mjs --tests-only",
    "check:list": "node scripts/run-checks.mjs --list"
  },
  "dependencies": {
    "redis": "^6.1.0"
  }
}
