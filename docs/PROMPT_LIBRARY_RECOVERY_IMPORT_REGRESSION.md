# Recovery import regression

Recovery import aşağıdaki akışa bağlıdır:

recovery JSON → normalizeRecoveryPayload → normal import plan → collision/capacity rules → explicit confirmation → merge

Recovery path ayrı bir storage formatı oluşturmaz.
