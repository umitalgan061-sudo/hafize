# Import legacy compatibility

Prompt Library import eski array formatını ve versioned wrapper formatını kabul eder.

Recovery wrapper ayrı bir helper üzerinden prompts alanını normal items alanına dönüştürür.

Hiçbir format eski storage anahtarını değiştirmez.

Collision ve capacity davranışı formatlar arasında aynıdır.

