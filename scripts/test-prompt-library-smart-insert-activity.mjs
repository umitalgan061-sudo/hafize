import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const source = await readFile(new URL('../public/prompt-library-smart-insert-activity.js', import.meta.url), 'utf8');
assert.match(source, /HISTORY_KEY/);
assert.match(source, /MAX_DAYS\s*=\s*14/);
assert.match(source, /MAX_ROWS\s*=\s*7/);
assert.match(source, /readHistory\(/);
assert.match(source, /usageFor\(/);
assert.match(source, /dailyFor\(/);
assert.match(source, /summary\(/);
assert.match(source, /exportDigest\(/);
assert.match(source, /Object\.freeze/);
assert.match(source, /replaceChildren/);
assert.match(source, /textContent/);
assert.match(source, /aria-labelledby/);
assert.match(source, /role.*status|setAttribute\(['"]role['"],\s*['"]status['"]\)/);
assert.doesNotMatch(source, /fetch\(|XMLHttpRequest|WebSocket|sendBeacon/);
assert.doesNotMatch(source, /innerHTML\s*=|outerHTML\s*=/);
assert.doesNotMatch(source, /entry\.body|entry\.values/);
console.log('prompt-library-smart-insert-activity: ok');
